name: clinsight_ai
version: "1.0.0"
config-version: 2

profile: clinsight_ai

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  clinsight_ai:
    staging:
      +materialized: view
      +schema: staging
